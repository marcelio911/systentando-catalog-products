import { ProductsIntegration } from './products-integration';

describe('ProductsIntegration', () => {
  it('should be defined', () => {
    expect(new ProductsIntegration()).toBeDefined();
  });
});
