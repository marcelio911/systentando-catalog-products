import { SuppliersIntegration } from './suppliers-integration';

describe('SuppliersIntegration', () => {
  it('should be defined', () => {
    expect(new SuppliersIntegration()).toBeDefined();
  });
});
