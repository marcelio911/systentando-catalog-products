import { SuppliersRepository } from './suppliers-repository';

describe('SuppliersRepository', () => {
  it('should be defined', () => {
    expect(new SuppliersRepository()).toBeDefined();
  });
});
